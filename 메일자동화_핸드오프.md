# 📬 정부양곡 명단 자동 이메일 추출 — 새 세션 핸드오프

> 작성: 2026-07-01 · 안토니(yyplus 메일 자동화 세션에서 인계)
> **새 세션에서 "이어서" 또는 이 문서를 읽고 시작.** 목표: 정부양곡 인수지시서·배송명단 메일을 자동 수집·추출해 wellshare-logis 프로그램에 반영.

---

## 0. 한눈 요약
- **대상 프로그램**: https://wellshare-logis.web.app (이 폴더 `I:\ttong_project\wellshare-logis-web`)
- **GitHub/계정**: `ttong627/wellshare-logis-web` — push 전 `gh auth switch --user ttong627`
- **Firebase**: `wellshare-logis` (#528541497350), Firebase Hosting(`firebase deploy --only hosting`)
- **스택**: React + TS + Vite + Firebase(firestore) · functions · mobile(기사앱) · ecount-gateway
- **⚠️ 절대규칙**: Firestore 월 문서 저장은 `updateDoc` 금지 → **`setDoc(..., {merge:true})` + 중첩객체** (CLAUDE.md)

## 1. 목표 (형 지시 2026-07-01)
**정부양곡 명단을 이메일에서 자동 추출**해서 wellshare-logis에 반영.
- 시청·구청이 보내는 **정부양곡 인수지시서 / 양곡 배송 명단** 메일을 자동 수집
- 첨부 엑셀(명단)을 추출 → 배송 대상자 명단으로 프로그램에 반영
- (yyplus 보건소 메일 자동화와 같은 철학: 수집→분류→적재→첨부/명단 추출)

## 2. ★재사용 자산 — yyplus 메일 시스템 (이미 완성·가동 중)
`I:\ttong_project\yyplus\wssc-unified\server\mail-collector\` 에 완성된 메일 자동화가 있다. **거의 그대로 복제·적응**하면 된다.

| 파일 | 역할 | 양곡에 적응할 점 |
|---|---|---|
| `worksmail.mjs` | 네이버웍스 Works API 전폴더 수집(페이지네이션·since) | **그대로 사용**(같은 메일함) |
| `auto-collect.mjs` | 무인 오케스트레이터(토큰 선제갱신3h·텔레그램 장애알림·12h중복방지). cron 2h | 그대로 패턴 사용. **토큰 IPv4 강제(net.setDefaultAutoSelectFamily(false)+dns ipv4first)** 주의 |
| `classify.mjs` | 분류(발주/민원/요청/청구)+보건소 매칭. **순수함수+테스트** | **양곡 분류기로 교체** — 인수지시서/명단/시청·구청 매칭 |
| `persist.mjs` | SA키 JWT→Firestore REST 적재(멱등 docId=mailId, updateMask 머지) | **target=wellshare-logis 프로젝트**로 변경(SA키·project_id) |
| `backfill-attachments.mjs` | Works 첨부→Storage 업로드→attachmentFiles | 그대로(양곡 명단 엑셀 첨부 수집) |
| `backfill-bodies.mjs` | 본문 bodyPreview 적재 | 그대로 |
| `decrypt-attachments.mjs` | 암호엑셀 KMS/비번 복호화 | 양곡 엑셀 암호화면 사용 |
| `notify-telegram.mjs` | ops 장애 텔레그램 알림(IPv4 픽스 포함) | 그대로 |
| `reconcile-audit.mjs` | 누락 대조감사+침묵경보 | 양곡용으로 적응 |

**메일함**: 정부양곡 메일은 **yyplus가 읽는 같은 네이버웍스 본사함(wssc.kr)·행복나눔함(hbnanum.com)에 들어온다**(이번 세션 실측 확인). 토큰: `~/.config/nworks/user-token.json`(본사) + `user-token.bucheon.json`(hbnanum). VM(tms-main-node)에 이미 상주 데몬 가동 중.

## 3. ★양곡 메일 특징 (이번 세션 실측 발굴)
yyplus에서 "양곡=노이즈"로 **제외한 바로 그 메일들**이 추출 대상이다.

**발신 (시청·구청 @korea.kr + 택배):**
- `yhr0304@korea.kr` — "26.X월 양곡배송 요청"
- `jjswp77@korea.kr` — "양곡 명단(오정구)"
- `ksh627@korea.kr` — "[원미구청] 26.X월 양곡배송 명단 송부"
- `koyoona@korea.kr` — "X월정부양곡 신청현황(경기도 시흥시청)"
- `labes9@korea.kr` — "경로당 정부양곡 X월 신청내역 송부"
- `pmspage@korea.kr` — "여주시 양곡 명단"(⚠️수지구보건소 주소와 동일인=주소만으론 영플/양곡 구분 불가, **내용으로 구분**)
- `social9@hanmail.net` — "정부양곡 인수지시서 그린택배(주)"
- 전달(FW): `@wssc.kr`(lhs3635·thsduddn), `hp34033660@daum.net`("정부양곡 매출지시서/인수지시서")

**제목 키워드 (분류기 시드):** `정부양곡` · `양곡` · `복지미` · `인수지시서` · `양곡대상자 명단` · `양곡 배송 명단` · `매출지시서` · `신청현황`

**지역 단위**: 보건소(yyplus)와 달리 **시청/구청/노인복지과** 단위(원미구·오정구·시흥·여주·하남·종로·중구·중원구·양평 등). wellshare-logis의 배송권역과 매핑 필요.

## 4. 핵심 차이 (yyplus vs 양곡)
| | yyplus(영양플러스) | wellshare-logis(정부양곡) |
|---|---|---|
| 대상 | 16개 보건소 발주/민원/요청/청구 | 시청·구청 양곡 인수지시서/명단 |
| Firestore | wellshare-tms | **wellshare-logis** (SA키 별도 필요) |
| 매칭 단위 | 보건소(clients) | **시청/구청/배송권역** |
| 저장규칙 | 평면 mailMessages | **월문서 setDoc(merge)** (CLAUDE.md 규칙) |
| 명단추출 | 발주서 검증화면 연결 | 인수지시서 엑셀 파싱→배송명단 |

## 5. 권장 시작 순서 (새 세션)
1. **`/확인`** — wellshare-logis-web 계정(ttong627)·동기화·배포환경 점검, PROJECT_STATUS 갱신
2. **`/plan`** — 설계: ①메일함 어디(yyplus와 공유 vs 별도) ②양곡 분류기 규칙 ③wellshare-logis Firestore 데이터모델(양곡 명단 어디 저장) ④인수지시서 엑셀 파싱(exceljs 이미 의존성) ⑤수집 데몬(yyplus VM 공유 vs 별도)
3. **결정 포인트(형 확인)**: 양곡 메일을 yyplus 데몬이 같이 추출해 wellshare-logis로 라우팅할지, 별도 데몬을 둘지. (같은 메일함이라 **한 데몬이 보건소→tms·양곡→logis 동시 라우팅**이 효율적)
4. 구현은 yyplus mail-collector 복제 → 양곡 분류기·logis 적재로 적응(TDD)

## 6. 관련 메모리/문서
- yyplus 메일 자동화 전체: 자동메모리 `project_yyplus_mail_automation`
- 정부양곡 5개 시스템 현황: 자동메모리 `project_yanggok_delivery_systems` (tms·nexus·logis-web·logis-system·희망나르미)
- 이 프로젝트: 자동메모리 `project_yanggok_mail_automation`(신규)
- yyplus 메일 코드(복제원본): `I:\ttong_project\yyplus\wssc-unified\server\mail-collector\`

## 7. 시크릿/주의
- 네이버웍스 토큰·SA키는 `~/.config/nworks`·`D:/Gemma4/_secrets`(yyplus와 공유). **wellshare-logis용 Firebase SA키는 별도 발급 필요**(현재 wellshare-tms SA만 있음).
- `.env`(이 폴더) 존재 — 값 비노출·커밋 금지.
- 동시편집 주의: yyplus 메일 코드를 복제할 때 yyplus 쪽은 건드리지 말 것(별도 진화 중).
